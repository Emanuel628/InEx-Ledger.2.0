/* =========================================================
   Subscription Page JS — V1 (public pricing view)
   ========================================================= */

document.addEventListener("DOMContentLoaded", () => {
  // Pricing is a public experience; no auth guards required.
});