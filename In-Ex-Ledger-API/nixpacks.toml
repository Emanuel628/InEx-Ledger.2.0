[phases.setup]
nixPkgs = ["nodejs"]

[phases.install]
cmds = ["npm ci", "npm --prefix frontend-v3 ci"]

[phases.build]
cmds = [
  "npm run build:frontend-v3",
  "node -e \"const fs=require('fs'); if (!fs.existsSync('public/app-v3/index.html')) process.exit(1)\""
]

[start]
cmd = "npm start"
