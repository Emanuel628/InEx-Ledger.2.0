name: Phase 7 Guardrails

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  enforce-export-guardrails:
    name: Enforce export guardrails
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: In-Ex-Ledger-API

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: In-Ex-Ledger-API/package-lock.json

      - name: Install dependencies
        run: npm --prefix In-Ex-Ledger-API install

      - name: Run export grant guard
        run: npm --prefix In-Ex-Ledger-API run test:export-grant

      - name: Run log scan
        run: npm --prefix In-Ex-Ledger-API run log_scan

      - name: Verify redacted export storage
        run: npm --prefix In-Ex-Ledger-API run verify:redacted-storage
