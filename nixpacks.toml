[phases.setup]
nixPkgs = ["nodejs"]

[phases.install]
cmds = ["cd In-Ex-Ledger-API && npm install"]

[start]
cmd = "cd In-Ex-Ledger-API && node scripts/repair-migration-checksums.js --write && npm start"
